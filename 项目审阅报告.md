# 项目审阅报告

审阅日期：2026-05-25

## 结论

当前项目已经具备“本地小说项目管理 + Codex 直连 + 本地模型备用 + 多阶段写作流水线 + 长期记忆 + 资料投喂 + 质检修订闭环”的主干能力，可以继续作为本地小说生产工具迭代。

主要问题不在功能缺少，而在入口逐渐变多、部分功能边界重叠、前后端文件偏大。后续不再无边界加功能，统一按 `ROADMAP.md` 分版本迭代；当前版本范围见 `CURRENT_ITERATION.md`。

## 本次已合并和清理

- 将“运行历史”并入“AI 任务中心”。Codex 运行记录、流水线状态同步、修订任务单、质检报告统一从任务中心查看。
- 删除旧的单步生成、润色、审稿、连续性检查按钮遗留脚本。当前写小说入口只保留“多阶段流水线”。
- 将原“生产闭环”拆成“修订发布”和“项目维护”。修订、批量质检、冲突看板、发布资料留在修订发布；模型预设和项目克隆移入项目维护。
- 建立 `09_运行时/tasks.json` 统一任务索引。质检、修订、发布资料、冲突、维护、流水线、创意孵化、资料投喂、Codex 直连和导出会写入任务中心。
- 增加“文风模仿”。作者样本会被整理为高层风格画像，写入 `05_提示词/文风模仿档案.md` 和 `04_连续性/style_memory.json`，运行时通过 `recalledMemory.styleImitation` 自动召回。
- 增强长期记忆检索。运行时会按当前 brief 排序召回人物状态、伏笔、剧情线程、读者承诺、相似章节和情绪记录，写入 `recalledMemory.ranked`。
- 增加“召回调试”面板。运行流水线前可按当前章节信息预览 `recalledMemory.ranked`，并可把关键条目钉选为本章必读，写入 `04_连续性/memory_pins.json` 和运行时 `recalledMemory.pinned`。
- 增加状态同步差异预览。应用 `state-sync.md` 前可查看目标文件、追加/合并方式、写入前数量和预计写入后数量。
- 增加召回结果手动排除。可把本章不该参与生成的人物、伏笔、剧情线或承诺写入 `04_连续性/memory_exclusions.json`，运行时从 `recalledMemory.ranked` 中过滤。
- 增加故事圣经编辑器。人物、剧情线、读者承诺和世界规则可通过表单维护，并写回对应长期记忆 JSON。
- 增加记忆冲突修复闭环。记忆冲突看板发现“人物状态重复”后，可生成修复方案、自动创建安全快照、合并重复人物状态并输出修复报告。
- 建立 `ROADMAP.md` 和 `CURRENT_ITERATION.md`，将后续工作拆成版本并按权重推进。
- 清理文档和内部提示词里的外部参考项目名称，避免产品和公开文档展示第三方项目。
- 更新烟测检查，避免后续把已移除的第三方名称重新带回。

## 仍有重叠但暂不建议删除

- 创作雷达与发布质检都涉及质量判断。雷达适合写作中途快速预警，发布质检适合生成硬性报告，建议保留，但可以在 UI 上把雷达定位成“质检前预扫”。
- 配置诊断与项目状态都属于健康信息。前者检查执行环境，后者检查项目内容，建议后续合并成“健康中心”的两个标签。
- 顶部快捷入口“最新运行时 / 最新 Codex 结果”和 AI 任务中心有部分重叠。快捷入口适合高频打开，任务中心适合看状态，建议保留。

## 缺少的核心能力

1. 任务队列增强
   已建立 `09_运行时/tasks.json` 任务索引，但还缺取消、失败重试、后台进度刷新和按任务类型筛选。

2. 长期记忆检索增强
   已有本地关键词排序召回、文风召回、召回调试面板、本章必读钉选和本章不读排除，但还缺向量检索、跨章节相似场景聚类。

3. 项目级规则版本
   资料投喂会更新能力包，但缺少规则版本、来源、启用/停用和回滚。建议为 `05_提示词/项目能力包.md` 增加变更记录和生效规则清单。

4. 故事圣经编辑器增强
   已有最小表单维护人物、剧情线、读者承诺和世界规则。后续可继续补时间线、伏笔、批量校验和删除/归档能力。

5. 冲突修复工作流增强
   已有“人物状态重复”的修复闭环，后续可继续补时间线重复、伏笔积压、未解决问题积压的半自动修复和人工确认。

6. 发布包最终检查
   已有 TXT / MD 导出，建议补“发布前总检查”：章节编号连续、标题缺失、字数异常、敏感限制提醒、简介/标签/封面描述齐全。

7. UI 自动化测试
   现有 smoke/regression 偏接口和标记检查。建议加入浏览器端最小回归：加载首页、切换左右栏、打开 AI 配置、切换工具分组、选择项目。

## 技术债优先级

高优先级：
- 拆分 `server.mjs`，按 project、ai-runner、pipeline、quality、memory、export 分模块。
- 拆分 `public/app.js`，按 state、api、renderers、actions、layout 分模块。
- 增强统一任务索引，补取消、失败重试、后台进度刷新和任务类型筛选。

中优先级：
- 继续细化右侧“更多工具”分组，减少低频工具对默认写作视图的干扰。
- 给核心文件写 schema 校验，避免 JSON 记忆被模型写坏。
- 增加项目导入/导出完整备份包。

低优先级：
- 更精细的主题切换。
- 更多平台模板。
- 桌面安装包签名和自动更新。

## 下一轮建议

下一轮建议进入 v0.4.0“任务队列版”。v0.3.0 的召回钉选、召回排除、状态同步差异预览、最小故事圣经编辑器和人物状态重复修复已经形成可控记忆闭环；下一步应补任务取消、失败重试、自动刷新和任务筛选。
