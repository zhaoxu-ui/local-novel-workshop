# 本地小说工坊使用教程

这份教程按真实写作流程来走。README 更像项目说明书，适合查功能；本教程适合第一次使用、边写边对照操作。

## 1. 启动项目

进入项目目录：

```powershell
cd <你的项目目录>
npm start
```

浏览器打开：

```text
http://127.0.0.1:8787
```

如果你在 Codex 应用里使用，直接打开本地页面即可。当前推荐本地 Node 启动，不优先使用 Docker，因为本地启动更方便调用宿主机的 `codex`、cc-switch 配置和资源管理器。

## 2. 先确认 AI 执行方式

页面顶部有“AI 执行配置”。

推荐选择：

```text
Codex 直连
```

这种模式会直接使用你本机的：

```text
C:\Users\你的用户名\.codex\config.toml
C:\Users\你的用户名\.cc-switch\settings.json
```

也就是说，如果你已经在本机用 cc-switch 切好了 Codex 的 provider/model，网页里不用再额外配置模型地址或密钥。

如果选择“本地模型接口”，才需要填写：

- 本地模型名，例如 `qwen3:8b`
- 本地模型接口，例如 `http://127.0.0.1:11434/api/generate`

保存后，配置会写入当前项目的 `project.json`。

顶部还可以隐藏/显示左侧项目栏和右侧工具栏。进入正文写作时，可以把两侧都收起来，让中间编辑区更清爽。

右侧工具区顶部默认只保留写作流程入口：

```text
写作流程 / 更多工具
```

日常写章节时停留在“写作流程”，只看章节看板、全局搜索、AI 任务中心和状态同步审核。需要档案助手、资料投喂、文风模仿、召回调试、快照、正文版本、发布质检、配置诊断或项目维护时，点“更多工具”，再切换到项目设置、质量与记忆、修订版本、发布准备或系统维护。

## 3. 创建小说项目

左侧“新建项目”填写：

- 项目名：这本书的内部名称
- 类型：悬疑、都市、同人、玄幻等
- 初始创意：一句话卖点、主角、核心困境、开局画面
- 档案助手：不确定就选“自动推荐”

点击：

```text
创建小说项目
```

项目会保存在：

```text
<你的项目目录>\projects
```

每个项目都是一个本地文件夹，不使用数据库。你可以直接打开里面的 Markdown 文件。

## 4. 用创意孵化生成立项资料

创建项目后，在左侧“创意孵化”里输入更完整的原始思路。

可以写得很随意，例如：

```text
我想写海贼王同人，主角穿越到东海，不想一开始就无敌。
希望主角有一个和原作人物不同的目标，不抢路飞主线，但能制造新的冲突。
风格热血一点，但不要像设定说明书。
```

点击：

```text
生成立项建议
```

它会根据“AI 执行配置”选择 Codex 直连或本地模型接口。

生成后重点查看中间上方“常用资料”里的：

- 立项建议
- 封面资料
- 创作总纲
- 主角卡
- 作者意图
- 当前焦点

这些文件会成为后续写作的基础资料。

## 5. 用章节看板管理整本书

右侧“章节看板”会显示当前项目的章节进度。

它会自动读取：

```text
00_总控/chapters.json
00_总控/章节目录.md
01_正文
```

每章会显示：

- 状态
- 章节号
- 标题
- brief
- 结尾钩子
- 是否已有正文
- 字数

章节状态包括：

```text
未规划 / 已规划 / 生成中 / 待审 / 可发布
```

如果你想先规划一章：

1. 在中间编辑区填写章节号。
2. 填写标题。
3. 填写本章 brief。
4. 点击“保存章节规划”。

保存后，它会写入：

```text
00_总控/chapters.json
00_总控/章节目录.md
```

点击看板里的任意章节，会把章节号、标题、brief 和已有正文载入编辑区。这样你可以按章节推进，而不是在文件列表里找来找去。

## 6. 全局搜索和任务中心

右侧“全局搜索”可以跨项目所有 Markdown / TXT / JSON / YAML 文件查关键词。适合找人物设定、伏笔、平台规则、Codex 输出和运行时产物。点击搜索结果会直接打开对应文件。

右侧“AI 任务中心”会汇总：

- Codex 直连任务
- Codex 运行历史
- 多阶段流水线
- 资料投喂
- 多阶段流水线状态同步审核
- 修订任务单
- 质检报告
- 发布资料和项目维护记录

它优先读取 `09_运行时/tasks.json` 统一任务索引，并兼容旧项目的已有产物扫描。这里不会自动触发模型；运行流水线、Codex 直连、资料投喂、修订、质检、发布或维护操作后，可以点“刷新任务”查看最新结果。

右侧“创作雷达”基于本地题材规则、套路风险、去 AI 腔规则和健康检查思路实现。它是本地轻量扫描，不调用模型，也不会改正文。它会给出：

- 人味：AI 腔连接词、套路句、机械动作词风险
- 题材：当前题材锚点是否足够，有没有串味词
- 节奏：长段落、对话占比、移动端可读性
- 连续性：未解决问题、伏笔、章节日志是否积压
- 发布：综合准备度评分

如果正在打开某一章正文，雷达会优先扫描当前章；否则扫描最近几章。

## 7. 资料投喂：把规则变成项目能力

如果你有平台规则、写作限制、审稿标准、自己的写作原则，可以在右侧“资料投喂”上传或粘贴。

适合投喂的内容：

- 番茄、起点、七猫等平台规则摘要
- 你自己的文风要求
- 禁止事项，例如不要洗稿、不要照搬原作台词
- 审稿标准，例如节奏、钩子、人物动机、AI 感问题
- 某类型小说的结构原则

点击：

```text
分析并吸收资料
```

注意：资料投喂不是“上传什么就无脑加入什么”。工具会让 AI 综合判断、去重、筛选、标记风险，只把适合当前项目的内容沉淀到：

```text
05_提示词/项目能力包.md
05_提示词/项目工作流.md
05_提示词/多角色协作.md
05_提示词/SKILL.md
```

原始资料会保存在：

```text
08_资料投喂/原始资料
```

处理报告会保存在：

```text
08_资料投喂/投喂报告_时间戳.md
```

## 8. 文风模仿：让流水线学你的项目语气

右侧“文风模仿”用于粘贴你自己的样稿，或本项目已经确认满意的正文片段。点击“生成文风模仿档案”后，会写入：

```text
05_提示词/文风模仿档案.md
04_连续性/style_memory.json
08_资料投喂/风格样本_时间戳.md
```

它只提取高层风格画像，例如节奏、句式、对话密度、细节偏好和禁用表达。不要投喂未授权作品当作仿写目标；系统也会避免照搬原句、特定作者签名风格、独创设定或具体桥段。

之后运行多阶段流水线时，`09_运行时/.../context.json` 会在 `recalledMemory.styleImitation` 中自动召回这份文风档案。

同一个 `recalledMemory` 里还会按本章 brief 排序召回：

- `ranked.characters`：相关人物状态
- `ranked.foreshadows`：相关伏笔
- `ranked.plotThreads`：相关剧情线程
- `ranked.readerPromises`：相关读者承诺
- `ranked.similarChapters`：相似章节日志
- `ranked.emotionBeats`：相关情绪记录

右侧“召回调试”可以在真正运行流水线前预览这些内容。填写章节号、标题和 brief 后，点击：

```text
预览本章召回
```

如果某条人物、伏笔、剧情线、读者承诺或情绪记录对本章特别重要，点击条目里的“钉选本章”。被钉选的内容会出现在“本章必读”区域，并写入：

```text
04_连续性/memory_pins.json
```

之后运行多阶段流水线时，`context.json` 会在 `recalledMemory.pinned` 中带上这些本章必读记忆。点“取消钉选”可以把它从当前章节移除。

如果某条召回结果本章不该用，点击“排除本章”。被排除的内容会出现在“本章不读”区域，并写入：

```text
04_连续性/memory_exclusions.json
```

之后运行多阶段流水线时，`context.json` 会在 `recalledMemory.excluded` 中记录这些排除项，并把它们从本章结构化召回结果中过滤掉。点“取消排除”可以恢复。

如果召回结果不对，先补人物状态、伏笔表、读者承诺或章节日志，再运行流水线。

## 9. 写一章：只走多阶段流水线

当前写作区只保留一个 AI 写作入口：

```text
运行多阶段流水线
```

不要再分开点“生成章节、润色、审稿、连续性检查”。这些步骤已经合进流水线里。

运行前建议先到右侧“项目保险箱”创建一份快照。备注可以写：

```text
第 3 章生成前
```

如果后续生成结果破坏了总纲、人物状态或连续性，可以直接恢复这个快照。

写章前，在中间编辑区填写：

- 章节号
- 标题
- 本章 brief

编辑区会自动保存当前 brief 和正文草稿到：

```text
09_运行时/autosave/current-draft.md
```

自动保存只是兜底备份，不会覆盖正式章节。真正要进入正文目录，仍然点击“保存为章节”。

本章 brief 建议包含：

```text
本章目标：
开场钩子：
主要冲突：
人物变化：
必须出现的信息：
不能写的内容：
结尾钩子：
```

示例：

```text
本章目标：主角第一次意识到这个世界不是照着原作走。
开场钩子：海边出现一艘不该出现在东海的船。
主要冲突：主角想避开麻烦，但麻烦主动找上门。
人物变化：从旁观原作，变成必须做选择。
必须出现的信息：主角能力只能解决局部问题，不能碾压。
不能写的内容：不要直接复述原作剧情，不要大段解释设定。
结尾钩子：船上有人认出了主角不该知道的名字。
```

点击：

```text
运行多阶段流水线
```

流水线会依次执行：

1. 规划：明确本章目标、冲突、信息增量、人物变化、结尾钩子。
2. 编排：安排场景顺序、信息释放、伏笔和情绪节奏。
3. 写作：生成正文草稿。
4. 审计：检查 AI 感、连续性、人物可信度、节奏、冲突、合规风险。
5. 修订：输出最终正文。
6. 状态同步：整理需要写回长期记忆的内容。

## 10. 查看流水线结果

运行后，右侧“状态同步审核”会出现流程图。

节点含义：

```text
意图 -> 上下文 -> 规则栈 -> 规划 -> 编排 -> 写作 -> 审计 -> 修订 -> 同步
```

颜色含义：

- 绿色：已完成
- 黄色：需要复核
- 红色：失败
- 灰色：未完成

下方“运行详情”会显示每个阶段的文件：

- `intent.md`：本轮任务意图
- `context.json`：读取了哪些项目资料，以及本轮自动召回的相关人物、伏笔、剧情线程、读者承诺、相似章节和文风档案
- `rule-stack.yaml`：本轮规则优先级
- `plan.md`：规划
- `orchestration.md`：编排
- `draft.md`：草稿
- `audit.md`：审计
- `revision.md`：修订稿
- `state-sync.md`：状态同步建议
- `trace.json`：运行记录和错误

如果流水线失败，优先打开：

```text
Trace
```

常见失败原因：

- 本地模型接口没启动，出现 `fetch failed`
- 当前选择了“本地模型接口”，但 Ollama/LM Studio 没开
- Codex 命令不可用
- 项目文件缺失或路径异常

如果你没有刻意使用本地模型，建议把“AI 执行配置”切回：

```text
Codex 直连
```

## 11. 审核状态同步，避免污染长期记忆

流水线的最后一步会生成状态同步建议，通常在：

```text
09_运行时/<本次运行>/state-sync.md
```

右侧“状态同步审核”里，如果某条记录旁边有：

```text
审核同步
```

点击它，页面会把同步建议拆成可勾选类别：

- 章节日志
- 情绪账本
- 伏笔回收
- 故事状态
- 人物状态
- 时间线
- 剧情线程
- 世界状态
- 风格记忆
- 读者承诺

每个类别下面还会继续拆成具体条目或字段，例如某一条读者承诺、某一个风格字段、某一个故事状态字段。建议先只勾选你确认无误的项目，再点击：

```text
预览差异
```

差异预览会显示目标文件、追加或合并方式、写入前数量和预计写入后数量。确认影响范围没问题后，再点击：

```text
应用选中
```

应用前系统会自动在“项目保险箱”创建快照。未勾选的条目不会写入长期记忆；如果 AI 输出不是合法 JSON，系统只提示人工整理，不会自动写入。

## 12. 保存章节

流水线产出的最终修订稿会显示在中间“正文 / 修订结果”里。

确认满意后点击：

```text
保存为章节
```

章节会保存到：

```text
01_正文
```

保存后可以在“项目文件”里直接打开章节文件。

## 13. 维护项目记忆

这个工具不是只生成一次正文，它会维护项目记忆。

右侧“故事圣经”可以表单化维护四类高频长期记忆：

- 人物：写入 `04_连续性/character_state.json`
- 剧情线：写入 `04_连续性/plot_threads.json`
- 读者承诺：写入 `04_连续性/reader_promises.json`
- 世界规则：写入 `04_连续性/world_state.json`

选择分区后可以刷新已有条目，点击条目会回填表单；修改 ID、名称、状态和摘要后点击“保存故事圣经条目”。如果 ID 已存在，会更新原条目；如果 ID 不存在，会新增条目。

重点文件：

```text
00_总控/author_intent.md
00_总控/current_focus.md
04_连续性/章节日志.md
04_连续性/情绪账本.md
04_连续性/伏笔回收表.md
04_连续性/story_state.json
04_连续性/character_state.json
04_连续性/timeline_state.json
04_连续性/plot_threads.json
04_连续性/world_state.json
04_连续性/style_memory.json
04_连续性/reader_promises.json
05_提示词/文风模仿档案.md
```

建议每隔几章人工看一遍：

- 人物有没有偏离
- 伏笔有没有丢
- 情绪账本有没有断
- 当前焦点是否还符合你想写的方向
- 作者意图是否需要更新

## 14. 使用项目保险箱

“项目保险箱”在右侧工具区。

推荐在这些操作前创建快照：

- 运行多阶段流水线前
- 资料投喂前
- 大改总纲前
- 大改人物卡、世界观、伏笔表前
- 准备恢复旧版本前

操作方式：

1. 在“快照备注”里写一句说明，例如 `第 5 章生成前`。
2. 点击“创建项目快照”。
3. 后续需要回退时，点击对应快照的“恢复”。

恢复快照会覆盖当前项目。为了防止误操作，系统会先自动创建一份“恢复前自动快照”，再执行恢复。

## 15. 运行发布前质检

右侧“发布质检”可以选择：

- 当前章节
- 全书

点击：

```text
运行发布前质检
```

系统会在本地扫描章节，不依赖模型。它会检查：

- 是否缺标题、brief、结尾钩子
- 正文字数是否过短或过长
- 是否有较多长段落
- 是否存在明显 AI 感风险词
- 是否存在段落起手式重复
- 连续性文件是否齐备

质检结果会给出：

```text
可发布 / 需小修 / 需重写
```

同时会保存一份报告到：

```text
06_发布/发布前质检_时间.md
```

这不是替代人工审稿，而是发布前的一道本地硬性检查。

创作雷达适合在正式质检前先跑一遍。它更像“早期预警”，发现 AI 腔、套路句、题材串味和记忆积压；发布前质检更偏硬性检查和报告产物。

如果还需要编辑级判断，可以继续点击：

```text
AI 深度复核
```

它会读取顶部“AI 执行配置”：

- 选择 `Codex 直连` 时，会启动本机 Codex 任务，按 cc-switch 当前配置生成深度质检报告。
- 选择 `本地模型接口` 时，会直接调用本地模型生成报告。

AI 深度复核会重点检查人物动机、情绪推进、剧情空转、说明书式设定、爽点/钩子、发布风险、同人照搬风险，并输出可交给 Codex 或多阶段流水线执行的修订任务单。

报告保存到：

```text
06_发布/AI深度质检_时间.md
```

## 16. 使用修订发布和项目维护

右侧“修订发布”把质检后的动作集中在一起：

- 生成修订任务单：读取最近的本地质检或 AI 深度质检报告，整理成 `06_发布/修订任务单_时间.md`。
- 一键修订流水线：读取修订任务单，按当前 AI 执行方式启动 Codex 或本地模型修订。修订前会自动创建项目快照，并把旧稿保存到 `01_正文/历史版本`。
- 批量质检：按起始章/结束章批量检查章节，并写入 `09_运行时/批量任务_时间.md`。
- 记忆冲突看板：生成 `04_连续性/记忆冲突报告_时间.md`，检查人物状态重复、时间线重复、伏笔积压和未解决问题积压。
- 记忆冲突修复：冲突报告卡片里如果发现“人物状态重复”，可以先生成修复方案，再应用修复。应用前会自动创建安全快照，应用后会合并重复人物状态并生成 `04_连续性/记忆冲突修复_时间.md`。
- 生成发布资料包：输出作品简介、标签、章节标题列表、封面提示词和平台注意事项。

右侧“项目维护”放低频操作：

- 保存当前模型预设：把当前 Codex/本地模型配置写入项目预设。
- 克隆当前项目：复制一份完整项目，适合做大改前分支。

“正文版本”区域可以刷新历史版本，打开旧稿，和当前正文对照，也可以恢复旧版本。恢复前系统会先创建快照并保存当前正文备份。

## 17. 导出发布稿

顶部提供：

- 导出 TXT
- 导出 MD

导出会读取：

```text
01_正文
```

按章节生成发布稿，保存到项目文件夹里。

## 18. 推荐日常流程

每一章推荐这样用：

1. 打开项目。
2. 看一眼“章节看板”“当前焦点”和“章节日志”。
3. 点击要写的章节，或新建一条章节规划。
4. 在“项目保险箱”创建快照。
5. 写本章 brief。
6. 点击“运行多阶段流水线”。
7. 看流程图和运行详情。
8. 重点检查 `audit.md` 和 `revision.md`。
9. 满意后保存为章节。
10. 必要时手动补充人物卡、伏笔表、情绪账本。
11. 下一章继续。

每个新项目推荐这样用：

1. 创建项目。
2. 生成立项建议。
3. 检查并修改创作总纲、主角卡、封面资料。
4. 投喂规则或写作原则。
5. 写第 1 章 brief。
6. 运行多阶段流水线。
7. 根据审计结果调整项目能力包或作者意图。

## 19. 常见问题

### 打开项目文件夹没反应

本地 Node 启动时会尝试调用系统资源管理器。若浏览器或系统限制导致无反应，可以点击：

```text
复制项目路径
```

然后手动粘贴到资源管理器地址栏。

### 生成流水线显示 fetch failed

通常是你选择了“本地模型接口”，但本地模型服务没有启动。

解决方式：

1. 切换到“Codex 直连”。
2. 点击“保存执行配置”。
3. 再运行多阶段流水线。

如果你确实想用本地模型，先启动 Ollama 或 LM Studio，并确认接口地址可访问。

### Codex 直连需要额外配置吗

不需要在网页里额外配置。它读取本机 Codex 和 cc-switch 当前配置。

只要你的命令行里：

```powershell
codex --version
```

能正常运行，且 cc-switch 已切好 Codex 配置，就可以直接用。

### 资料投喂会不会污染项目

设计上不会直接“投什么吸收什么”。AI 会生成判断报告，并把可采纳内容整理到项目能力文件里。

但重要规则仍建议人工看一眼：

```text
08_资料投喂/投喂报告_时间戳.md
05_提示词/项目能力包.md
```

更稳妥的做法是：投喂前先创建项目快照，确认吸收结果没问题后再继续写作。

### 我该主要看哪些文件

日常最常看：

```text
00_总控/立项建议.md
00_总控/创作总纲.md
00_总控/chapters.json
00_总控/current_focus.md
02_人物/主角.md
04_连续性/章节日志.md
04_连续性/伏笔回收表.md
05_提示词/项目能力包.md
09_运行时/.../audit.md
09_运行时/.../revision.md
```

## 20. 和 README 怎么配合看

- 想知道项目有哪些能力：看 `README.md` 的“当前能力”。
- 想知道目录结构：看 `README.md` 的“项目位置”。
- 想理解控制面、长期记忆、运行时产物：看 `README.md` 对应章节。
- 想照着操作：看本教程。

简单说：

```text
README.md = 项目说明书
使用教程.md = 操作手册
```

## 21. 发布或打包前自检

准备发到 GitHub 或重新打 Windows 安装包前，建议在项目根目录执行：

```powershell
npm run check
npm run smoke
npm run regression
npm run prepare:github
```

`regression` 会用临时项目目录走一遍核心接口，不会改你的真实小说项目；`prepare:github` 会生成 `GITHUB_RELEASE_CHECK.md`，方便确认 `projects/`、`.codex/`、`.cc-switch/`、`.env`、`node_modules/` 等本地内容没有进入发布范围。
