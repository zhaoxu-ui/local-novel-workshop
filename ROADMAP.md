# 版本迭代路线图

本路线图用于控制功能范围，避免项目无边界地持续加功能。每个版本只解决一组主要问题；不在当前版本范围内的需求先进入后续版本候选。

## 版本总览

| 版本 | 权重 | 主题 | 目标 |
| --- | ---: | --- | --- |
| v0.2.0 | 100 | 创作闭环稳定版 | 固化现有核心链路，冻结范围，补齐文档、测试和发布检查。 |
| v0.3.0 | 92 | 可控记忆版 | 增强召回结果控制、状态同步安全和故事圣经编辑。 |
| v0.4.0 | 86 | 任务队列版 | 增加任务取消、失败重试、自动刷新和任务筛选。已完成。 |
| v0.5.0 | 78 | 发布准备版 | 增加发布前总检查、完整备份包和安装包发布规范。已完成。 |
| v0.6.0 | 70 | 工程治理版 | 拆分大型文件，补 UI 自动化测试和 JSON schema 校验。已完成。 |
| v0.7.0 | 68 | 真实创作验收版 | 增加演示小说项目和 10 章创作验收闭环。已完成。 |
| v0.8.0 | 66 | 文稿质量增强版 | 增加降 AI 感、钩子、情绪和口吻专项检查。已完成。 |
| v0.9.0 | 64 | 项目安全与迁移版 | 增加迁移包、记忆修复和项目损坏诊断。已完成。 |

## v0.2.0 创作闭环稳定版

已完成。这个版本不再主动扩展新大功能，重点是把已经形成的核心闭环变成可发布、可维护、可回归的稳定基线。

权重排序：

| 权重 | 项目 | 状态 |
| ---: | --- | --- |
| 100 | 多阶段流水线作为唯一写作入口 | 已完成 |
| 98 | Codex 直连与本地模型统一配置 | 已完成 |
| 96 | AI 任务中心统一读取 `09_运行时/tasks.json` | 已完成 |
| 94 | 长期记忆排序召回与召回调试 | 已完成 |
| 92 | 文风模仿档案与风格记忆 | 已完成 |
| 90 | 发布质检、修订发布、项目维护分区 | 已完成 |
| 88 | README、使用教程、项目审阅报告同步 | 已完成 |
| 86 | GitHub 发布检查不携带本地配置和第三方展示 | 已完成 |
| 84 | 全量验证命令稳定通过 | 已完成 |

v0.2.0 冻结范围：

- 只修复阻断使用、测试失败、文档不一致、发布风险和明显 UI 错误。
- 不新增新的右侧大面板，除非是修复当前闭环不可用。
- 不引入云服务、数据库或额外部署依赖。
- 不做大规模重构，避免在稳定版里制造风险。

v0.2.0 出口标准：

```powershell
npm run check
npm run smoke
npm run regression
npm run prepare:github
```

以上命令全部通过，并确认 `README.md`、`使用教程.md`、`ROADMAP.md`、`CURRENT_ITERATION.md` 与实际界面一致。

## v0.3.0 可控记忆版

目标：让 AI 写作前读取什么、写作后同步什么，都可视、可控、可回退。

候选项按权重排序：

| 权重 | 候选项 |
| ---: | --- |
| 92 | 召回结果手动钉选：把人物、伏笔、承诺固定为本章必读。已完成。 |
| 90 | 状态同步写入前差异预览：显示将修改哪些文件和字段。已完成。 |
| 88 | 故事圣经编辑器：人物、剧情线、读者承诺、世界规则表单化维护。已完成。 |
| 84 | 召回结果手动排除：临时禁止某条记忆参与本章生成。已完成。 |
| 80 | 记忆冲突修复闭环：冲突报告到修复方案再到应用。已完成。 |

## v0.4.0 任务队列版

目标：让任务中心从“产物列表”升级为可操作队列。

候选项按权重排序：

| 权重 | 候选项 |
| ---: | --- |
| 86 | 运行中自动刷新任务状态。已完成。 |
| 84 | 失败任务一键重试。已完成。 |
| 82 | Codex 任务取消。已完成。 |
| 78 | 按任务类型、状态筛选任务中心。已完成。 |
| 74 | 任务详情抽屉，集中查看日志、产物、错误和下一步。已完成。 |

## v0.5.0 发布准备版

目标：从“能生成小说”推进到“能放心整理发布”。

候选项按权重排序：

| 权重 | 候选项 |
| ---: | --- |
| 78 | 发布前总检查：章节连续、标题、字数、简介、标签、封面描述。已完成。 |
| 76 | 项目完整备份包：导出项目资料和正文，排除本地敏感配置。已完成。 |
| 72 | 发布包清单：TXT/MD、简介、标签、封面提示词、注意事项统一打包。已完成。 |
| 70 | Windows 安装包版本号、更新日志和发布模板规范化。已完成。 |

## v0.6.0 工程治理版

目标：降低维护成本，减少后续改动风险。

候选项按权重排序：

| 权重 | 候选项 |
| ---: | --- |
| 70 | 拆分 `server.mjs` 为 project、memory、pipeline、quality、export、codex-runner。已完成。 |
| 68 | 拆分 `public/app.js` 为 api、state、render、actions、panels。已完成。 |
| 66 | 增加浏览器 E2E 测试。已完成。 |
| 64 | JSON schema 校验长期记忆文件。已完成。 |
| 60 | 错误日志面板和故障诊断报告。已完成。 |

## v0.7.0 真实创作验收版

目标：用内置演示项目验证真实写作链路是否顺手。

候选项按权重排序：

| 权重 | 候选项 |
| ---: | --- |
| 68 | 一键体验/演示项目。已完成。 |
| 66 | 10 章创作验收目录、样章和验收报告。已完成。 |
| 64 | 演示项目长期记忆、封面资料和发布资料。已完成。 |
| 62 | 页面入口和回归测试覆盖演示项目。已完成。 |

## v0.8.0 文稿质量增强版

目标：把质检从硬性发布检查推进到正文可读性和人味专项修订。

候选项按权重排序：

| 权重 | 候选项 |
| ---: | --- |
| 66 | 降 AI 感专项修订。已完成。 |
| 64 | 章节爽点/钩子/情绪推进评分。已完成。 |
| 62 | 人物口吻一致性检查。已完成。 |
| 60 | 文稿质量增强报告和任务中心记录。已完成。 |

## v0.9.0 项目安全与迁移版

目标：让项目能跨电脑迁移，并能在损坏时自检和修复。

候选项按权重排序：

| 权重 | 候选项 |
| ---: | --- |
| 64 | 项目导入/导出。已完成。 |
| 62 | 备份恢复向导清单。已完成。 |
| 60 | 记忆 JSON 自动修复。已完成。 |
| 58 | 项目损坏诊断。已完成。 |

## 版本推进规则

- 当前版本未达到出口标准时，不启动下一版本。
- 新需求先判断是否属于当前版本；不属于则进入路线图候选。
- 同一版本只允许少量高权重需求进入实现。
- 每个版本结束时更新 `CURRENT_ITERATION.md`，再决定是否进入下一版本。
